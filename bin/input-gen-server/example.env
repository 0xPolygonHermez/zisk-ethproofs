# Set the following environment variables and rename/copy the file to .env

# URLs for the Ethereum node RPC and WebSocket connections
RPC_URL=http://localhost:8545
RPC_WS_URL=ws://localhost:8546

# WS Port
WS_PORT=8765

# Folder where the block inputs files are stored
INPUTS_FOLDER=inputs

# Modulus to apply to select blocks for input file generation
BLOCK_MODULUS=1
